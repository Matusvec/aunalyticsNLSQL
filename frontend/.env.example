# Copy to `.env.local` for local development.
# FastAPI default from ./start_dev_services.sh
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: simulate upload without calling POST /api/upload (UI testing only)
# NEXT_PUBLIC_MOCK_UPLOAD=true
